[pytest]
# Where to find tests
testpaths = tests

# Show extra detail on failures
addopts = -v --tb=short

# What files count as test files
python_files = test_*.py

# What functions count as tests
python_functions = test_*

# What classes count as test classes
python_classes = Test*